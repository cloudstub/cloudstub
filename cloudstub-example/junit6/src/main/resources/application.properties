# Default endpoint for local/demo runs against a standalone CloudStub server.
# Tests override this: CloudStubExtension sets aws.endpoint-url as a system property
# (its embedded port), which outranks this file in Spring's property precedence.
aws.endpoint-url=http://localhost:4566
