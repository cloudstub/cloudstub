# No aws.endpoint-url here: with no profile the SDK uses real AWS endpoints.
# The `local` profile points the clients at a standalone CloudStub server; the
# `prod` profile is the explicit real-AWS counterpart. Tests need neither — the
# CloudStubExtension injects aws.endpoint-url as a system property at runtime.
