name: Deploy docs

on:
  push:
    branches: [main]
    paths:
      - "docs/gh-pages/**"
      - "mkdocs.yml"
      - "CHANGELOG.md"
      - ".github/workflows/docs.yml"
      # Module source feeds the aggregated Javadoc, so a source change redeploys the site to keep
      # the API reference in sync.
      - "**/src/main/java/**"
      - "build.gradle"
      - "gradle/libs.versions.toml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v5.2.0
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v5.0.2
        with:
          gradle-version: "8.8"

      - uses: actions/setup-python@v6.0.0
        with:
          python-version: "3.x"

      - name: Install MkDocs Material
        run: pip install mkdocs-material

      - name: Generate Javadoc
        run: |
          gradle generateDocs
          rm -rf docs/gh-pages/javadoc
          cp -r docs/javadoc docs/gh-pages/javadoc
          # Javadoc bundles third-party license texts as Markdown (legal/*.md). MkDocs would render
          # them as orphan pages; drop them so only the API HTML/CSS/JS is published.
          find docs/gh-pages/javadoc -name '*.md' -delete

      - name: Copy changelog into site
        run: cp CHANGELOG.md docs/gh-pages/changelog.md

      - name: Deploy to GitHub Pages
        run: mkdocs gh-deploy --force
