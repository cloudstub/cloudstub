# Published Maven coordinate shared by every module (see issue #78). The group is the
# GitHub-org-verified Central namespace; `version` stays a -SNAPSHOT during development and is
# overridden at release time (e.g. -Pversion=0.1.0-beta.1).
group=io.github.cloudstub
version=0.1.0-SNAPSHOT
